global:
  resolve_timeout: 5m

route:
  receiver: default-webhook
  group_by: ['alertname', 'tenant']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: default-webhook
    webhook_configs:
      - url: http://webhook-sink:8080/alert
        send_resolved: true
